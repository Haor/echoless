LocalVQE native runtime libraries copied here by `pnpm prepare:tauri-assets`.

This directory should contain the platform `liblocalvqe`/`localvqe.dll` library
and companion GGML backend libraries when the build environment provides them.
