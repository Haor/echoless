# Echoless Audit Progress

Audit source: `docs/audit/CODE_AUDIT.md`

Baseline branch: `phase-0/green-baseline`

Last updated: 2026-06-10

## Status Ledger

| ID | Phase | Status | Closing commit | Verification |
| --- | --- | --- | --- | --- |
| P0.0 | green baseline | done | `7406a14` | `cargo clippy --workspace --all-targets --locked -- -D warnings`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)`; `cargo test --workspace --locked`; `(cd app/src-tauri && cargo build --locked)` |
| TEST-1 | P0 safety net | done | `ee8999a` | CI now includes Tauri backend clippy/build, frontend `tsc --noEmit`, and `pnpm tauri build --debug --no-bundle --ci`; local Tauri build smoke passed |
| TEST-2 | P0 safety net | done | `ee8999a` | CI now includes first-party Rust fmt checks and root/app/vendor cargo-audit; local scoped fmt and cargo-audit runs passed |
| CFG-1 | P0 safety net | done | `ee8999a` | CI pins LocalVQE checkout to `de56a174d9662b65f404ec65ae8e4bc9712db0da` |
| RUNTIME-1 | P1 usable | done | `cdcddb8` | `started` event exposes `cli_version` and `supported_controls`; frontend guards diagnostics/output-level stdin commands; `cargo test -p echoless-cli runtime_control --locked`; `pnpm exec tsc --noEmit` |
| RUNTIME-2 | P1 usable | doing | `0d5e8cc`, `75a7ced`, `f639c05`, `3706757`, `1bf38fa` | Tauri `localvqe_assets` now reports native readiness/library path; `echoless_command` injects `ECHOLESS_LOCALVQE_LIBRARY` and native library search env; frontend gates LocalVQE READY on model + native runtime. Real macOS LocalVQE v1.3 resources passed direct `localvqe_ffi_smoke`; repo-native `pnpm smoke:tauri-bundle` runs bundled sidecar LocalVQE offline from `Echoless.app/Contents/Resources/resources/localvqe`; `smoke-tauri-bundle.mjs --installed-app <dir>` now verifies the installed app's sidecar CLI, bundled model/native library, processor manifest, and offline LocalVQE output. Windows CI is wired to build the Tauri NSIS installer, run `smoke-windows-installed-app.ps1`, and delegate to that installed-app smoke. Remaining: run the Windows CI/real Windows installation and attach `bundle-smoke: ok` output |
| PKG-1 | P1 usable | doing | `0d5e8cc`, `75a7ced`, `f639c05`, `3706757`, `1bf38fa` | `bundle.externalBin`/`resources` configured; `pnpm prepare:tauri-assets` generates CLI sidecar, Process Tap helper resource, and LocalVQE resource copies when assets exist/reused; `pnpm tauri build --debug --ci --bundles app` produced macOS `Echoless.app`; bundle contains executable sidecar, ad-hoc-signed Process Tap helper, Info.plist microphone/system-audio descriptions, and LocalVQE model/native libs. CI includes macOS bundle smoke and now has a Windows NSIS installer-install-smoke path after LocalVQE assets are prepared; locally it passed against macOS `.app` plus a temporary installed-app-equivalent layout. `docs/windows-testing/WINDOWS_INSTALLED_APP_SMOKE_HANDOFF.md` is the Windows close path. Remaining: full Windows installed-app bundle smoke from the actual installed directory |
| ARCH-1 | P2 split | done | `3c611a6`, `97ca670`, `53d6223`, `532c123`, `5108bf5`, `8a232f0`, `e47f3e3`, `411e7b6`, `58d4345`, `6fcdbc8` | Completed mechanical split by moving runtime stdin control parsing/dispatch into `realtime/control.rs`, device I/O linear resamplers into `realtime/resample.rs`, live status aggregation into `realtime/stats.rs`, diagnostic recorder/writer handling into `realtime/diagnostics.rs`, device selection/doctor helpers into `realtime/devices.rs`, CLI arg definitions into `cli.rs`, `probe-delay` CLI/DSP helpers into `probe_delay.rs`, config validation helpers into `config_validate.rs`, RTX AEC nvafx helpers into `nvafx_install.rs`, and remaining offline/processor/audio/run helpers into their own modules. `main.rs` is now a 40-line dispatcher. Verified with targeted control/resampler/stats/diagnostic/device/probe/config/nvafx/run/manifest tests plus `cargo clippy --workspace --all-targets --locked -- -D warnings` and `cargo test --workspace --locked`. |
| PERF-1 | P3 realtime | done | `59a9394`, `0d3c0fe` | `ProcessorChain` now owns per-node reusable near/far/output adapters and scratch buffers; an isolated counting-allocator integration test proves warmed 48k -> 16k -> 48k `ProcessorChain::process` performs zero heap allocations. Verified with `cargo test -p echoless-processors --test processor_chain_alloc --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| QUAL-1 | P3 realtime | done | `59a9394` | Node-boundary SRC now uses persistent rubato `FftFixedIn` resamplers with reusable planar buffers, replacing stateless per-block linear interpolation; stereo far reference remains distinct through 48k -> 16k SRC. Verified with `cargo test -p echoless-processors chain::tests --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| QUAL-3 | P3 realtime | done | `2474112` | LocalVQE now accumulates near/far samples in `VecDeque` and reuses a persistent `frame_out` buffer instead of `Vec::drain(..hop)` plus per-process frame allocation. Verified with `cargo test -p echoless-processors localvqe --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| QUAL-4 | P3 realtime | done | `cecc802` | `RealtimeStats` now maintains fixed-size online peak buckets for mic/ref/out waveforms instead of storing all interval samples before status emission; status JSON keeps the same 64-value arrays. Verified with `cargo test -p echoless-cli realtime::stats --locked`; `cargo fmt -p echoless-cli --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| ROB-2 | P3 realtime | done | `e937bdf` | `request_finish` now preserves the diagnostic writer `JoinHandle` so `Drop` can join and finalize WAV/stats files; regression test covers stop-triggered finalize without leftover `.part` files. Verified with `cargo test -p echoless-cli diagnostic_recorder --locked`; `cargo fmt -p echoless-cli --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| SON-1 | P3 realtime | done | `da2c04a` | Sonora backend `Result` errors now update `runtime_error_count` and `last_backend_error` through `ProcessorStats`, with capture-error bypass fallback and regression coverage for a reachable backend error. Verified with `cargo test -p echoless-processors sonora_backend_errors_are_reported_in_stats --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` |
| SEC-1 | P4 hardening | done | `a959003` | `open_url` now rejects empty, non-HTTP(S), hostless, whitespace, and control-character URLs before spawning a browser launcher; Windows no longer routes browser opens through `cmd /C start` and uses `rundll32.exe url.dll,FileProtocolHandler` instead. Verified with `(cd app/src-tauri && cargo test --locked)` and `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)` |
| SEC-2 | P4 hardening | done | `4d332cf` | `nvafx download-install` now treats embedded SHA256 pins as the trust anchor for `DEFAULT_NVAFX_RELEASE_TAG`; downloaded `SHA256SUMS.txt` entries for pinned assets are only accepted when they match the embedded pin, while custom tags can still use release sums. Verified with `cargo test -p echoless-cli nvafx_install --locked`; `cargo test -p echoless-cli --locked`; `cargo fmt -p echoless-cli --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings` |
| SEC-3 | P4 hardening | done | `9a0d166` | Tauri `validate_config` and `start_run` now write config TOML files under app-local `runtime-configs/` with unique process/time names and `create_new` exclusive creation; validate configs are cleaned after validation and run configs are tied to `RunChild` cleanup on stop/restart/exit/window close. Verified with `(cd app/src-tauri && cargo test --locked)`; `(cd app/src-tauri && cargo fmt --check)`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)` |
| SEC-4 | P4 hardening | done | `877f070` | Default LocalVQE dynamic-library discovery now searches only the executable directory and its `localvqe/` child, not CWD or `CWD/localvqe`; explicit relative `library = ...` keeps its CWD fallback. Windows LocalVQE loading also installs a temporary DLL directory guard for the candidate library parent. Verified with `cargo test -p echoless-processors localvqe --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings` |
| SEC-5 | P4 hardening | done | `f1f0016` | `download_localvqe_model` now only accepts pinned LocalAI-io/LocalVQE GGUF filenames, downloads from HF revision `5760d09ce556750f76c1251c024e4a8c37231591`, verifies expected file size and SHA256 before rename, reuses only already-valid cached models, and deletes failed `.part` downloads. Pins come from Hugging Face API `siblings[].lfs.sha256`; local bundled v1.3 hash was cross-checked. Verified with `(cd app/src-tauri && cargo test --locked)`; `(cd app/src-tauri && cargo fmt --check)`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)`; `shasum -a 256 app/src-tauri/resources/localvqe/models/localvqe-v1.3-4.8M-f32.gguf` |
| SEC-6 | P4 hardening | done | `2ce2e0b` | NvAFX doctor now resolves `nvidia-smi` from `%ProgramFiles%/NVIDIA Corporation/NVSMI` and `%SystemRoot%/System32` before PATH fallback; Windows system DLL checks search `%SystemRoot%/System32` and `SysWOW64` before PATH and report the resolved DLL path in doctor details. Verified with `cargo test -p echoless-processors nvafx --locked`; `cargo test -p echoless-processors --locked`; `cargo fmt -p echoless-processors --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings` |
| ROB-1 | P4 hardening | done | `a567cd1` | Tauri one-shot CLI/curl commands now run in blocking workers with bounded timeouts, killed hung children, checked exit status, and surfaced stderr; covered by `command_timeout_kills_hung_child`. Verified with `(cd app/src-tauri && cargo test --locked)`; `(cd app/src-tauri && cargo fmt --check)`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)` |
| ROB-3 | P4 hardening | done | `78d07e9` | Tauri run-state locking now recovers poisoned mutexes via `run_state_guard` instead of panicking; regression test `run_state_guard_recovers_poisoned_lock` covers the poisoned path. Verified with `(cd app/src-tauri && cargo test --locked)`; `(cd app/src-tauri && cargo fmt --check)`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)` |
| ROB-4 | P4 hardening | done | `78d07e9` | Runtime stdout/stderr readers now explicitly report line-read errors through `echoless://log` and stop the reader instead of silently dropping failures; grep confirms no `lines().flatten()`/`lines().map_while(Result::ok)` remains in the targeted Tauri/CLI paths. Verified with `(cd app/src-tauri && cargo test --locked)`; `(cd app/src-tauri && cargo fmt --check)`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)` |
| QUAL-2 | P4 hardening | done | `91e19fb` | `PipelineConfig::frame_size()` now computes in `u64`, clamps to `u32::MAX`, and preserves a minimum frame size of 1; regression coverage includes normal, zero, and extreme values. Verified with `cargo test -p echoless-core --locked`; `cargo fmt -p echoless-core --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings` |
| SON-2 | P4 hardening | done | `5144807` | Production assert scan over `sonora-aec3`/`sonora-ns`/`sonora-agc2` found no runtime `assert!`/`assert_eq!` outside test modules, only compile-time `const assert` guards in AGC2 RNN-VAD. Sonora AEC3 now reuses `EchoRemover` scratch buffers, avoids temporary sub-frame view allocation in `FrameBlocker`, and removes a stereo residual-power `to_vec`; `sonora/tests/realtime_alloc.rs` proves warmed mono and stereo-reference AEC3 render/capture paths allocate zero times. Verified with `cargo test -p sonora --test realtime_alloc --locked`; `cargo test -p sonora -p sonora-aec3 --locked`; `cargo clippy -p sonora -p sonora-aec3 --all-targets --locked -- -D warnings`; root `cargo test --workspace --locked`; root `cargo clippy --workspace --all-targets --locked -- -D warnings`; `git diff --check`; `graphify update echoless` |
| SON-3 | P4 hardening | done | `92161c1` | CI now runs `cargo test -p sonora -p sonora-aec3 --locked` and `cargo clippy -p sonora -p sonora-aec3 --all-targets --locked -- -D warnings` inside `vendor/sonora`; the vendored lockfile was refreshed for the current trimmed workspace, and minimal Sonora clippy fixes make the gate enforceable. Verified with those two vendor commands plus `cargo fmt -p sonora-agc2 -p sonora-aec3 --check`, `git diff --check`, and `graphify update echoless` |
| DOC-1 | P4 hardening | done | `411e686` | NvAFX default-release SHA256 pins now live in one `NVAFX_DEFAULT_RELEASE_PINS` table with structural tests, and `docs/research/nvafx_runtime_pin_rotation.md` documents trust model plus release rotation steps; frontend RTX handoff now states embedded pins are authoritative for the default tag. Verified with `cargo test -p echoless-cli nvafx_install --locked`; `cargo fmt -p echoless-cli --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings` |
| FE-1 | P5 frontend | done | `63cc97b` | Overview waveform canvases now cache canvas sizing with `ResizeObserver`, stop rAF work while hidden, draw backend-provided real waveforms only on status revisions, and keep continuous animation only for the active synthetic fallback path; footer bars also stop when hidden/stopped. Verified with `pnpm -C app build`; `git diff --check`; `graphify update echoless` |
| FE-2 | P5 frontend | done | `e776877` | Runtime live/health snapshots now live in a `useSyncExternalStore` telemetry store; App publishes status events without `setLive`/`setHealth`, and overview status, signal, footer bars, plus diagnostics subscribe at memoized rendering boundaries. Verified with `pnpm -C app build`; grep confirms App no longer has `setLive`/`setHealth`/`useState<Live|Health>`; `git diff --check`; `graphify update echoless` |
| LAT-1 | P5 frontend | done | `7b799f9` | Runtime `estimated_user_latency_ms` now includes `mic_q_samples` as `input_queue_latency_ms`, status JSON and diagnostics `stats.csv` expose the input queue contribution, frontend TS/docs mirror the field, and the overview label is narrowed to `Pipeline` / `管线延迟` instead of implying total end-to-end latency. Verified with `cargo test -p echoless-cli realtime::stats --locked`; `cargo test -p echoless-cli realtime::diagnostics --locked`; `cargo fmt -p echoless-cli --check`; `cargo clippy -p echoless-cli --all-targets --locked -- -D warnings`; `pnpm -C app build`; `git diff --check`; `graphify update echoless` |
| QUAL-5 | P5/P7 cleanup | done | `9f13975` | Processor copy-or-zero helpers now live in one crate-local DSP helper, CLI RMS dBFS math now lives in one CLI-local DSP helper, and resamplers remain intentionally separate because device-boundary and processor-boundary SRC have different state/latency contracts. Verified with `cargo fmt -p echoless-processors -p echoless-cli --check`; duplicate helper grep; `git diff --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `graphify update echoless` |
| ARCH-2 | P6 architecture | done | `d12de2f` | Removed the unused `ControlApi` trait and `run_realtime` stub from `echoless-core`, dropped the now-unused `crossbeam-channel` dependency, and updated README/crate metadata so core is described as config/offline/shared DSP utilities while realtime control remains the CLI sidecar contract. Verified with `rg -n 'ControlApi|run_realtime' crates README.md`; `rg -n 'crossbeam-channel' Cargo.toml crates Cargo.lock`; `cargo fmt -p echoless-core --check`; `cargo test -p echoless-core --locked`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `git diff --check`; `graphify update echoless` |
| ARCH-3 | P6 architecture | done | `edfa026`, `00eb13b`, `e981f39`, `22f54cd` | Chose to keep realtime in the CLI sidecar and add explicit stdin hot controls for safe parameters. Proof points now include `set_near_delay_ms`, which retunes the near-delay buffer inside the processing loop; AEC3 `set_initial_delay_ms`, which forwards a stream-delay hint through `ProcessorChain::set_stream_delay_ms`; AEC3 `set_aec3_ns` / `set_aec3_agc`, which apply Sonora top-level APM config through `ProcessorChain::set_runtime_param`; and LocalVQE `set_localvqe_noise_gate`, which calls the loaded runtime's `localvqe_set_noise_gate` without rebuilding streams or processors. AEC3 `tail_ms`, `delay_num_filters`, `linear_stable_echo_path`, LocalVQE model/runtime selection, and NvAFX load-time parameters remain restart-required unless future backend evidence proves safe loaded-state mutation. Verified with `cargo test -p echoless-cli realtime::control --locked`; `cargo test -p echoless-processors chain::tests --locked`; `cargo test -p echoless-processors sonora_aec3 --locked`; `cargo test -p echoless-processors localvqe --locked`; `cargo fmt -p echoless-processors -p echoless-cli -p echoless-core --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `pnpm -C app build`; `git diff --check`; `graphify update echoless` |
| FE-3 | P6 architecture | done | `edfa026`, `00eb13b`, `e981f39`, `22f54cd` | Frontend now treats `near_delay_ms`-only patches, AEC3 `initial_delay_ms`, AEC3 `ns/ns_level`, AEC3 `agc`, and LocalVQE `noise_gate/noise_gate_threshold_dbfs` edits as runtime hot controls when the running CLI advertises matching `supported_controls`, while preserving restart behavior for sample rate, frame size, reference channels, devices, engine, model/runtime, builder-level AEC3 params, and NvAFX load-time params. Future restart reduction must start with a new backend `supported_controls` proof point. Verified with `pnpm -C app build`; backend control tests listed under ARCH-3 |
| TEST-3 | P7 cleanup | done | `b074fd9` | Chain tests now cover 48k -> 16k processor-boundary resampling, warmed adapter reuse, stereo far reference preservation, and direct BoundaryAdapter stereo->mono / mono->stereo channel adaptation; core tests cover frame-size overflow and output-level mute/unity/boost/soft-limit plus out-of-contract `u32::MAX` clamping. Verified with `cargo test -p echoless-processors chain::tests --locked`; `cargo test -p echoless-core --locked`; `cargo fmt -p echoless-processors -p echoless-core --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `git diff --check`; `graphify update echoless` |

## Notes

- 2026-06-10 local gate refresh after `c902e80`: `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `(cd app/src-tauri && cargo clippy --all-targets --locked -- -D warnings)`; `(cd app/src-tauri && cargo build --locked)`; `pnpm -C app build`; `cargo fmt -p echoless-audio-io -p echoless-core -p echoless-processors -p echoless-cli --check`; `(cd app/src-tauri && cargo fmt --check)`; `(cd vendor/sonora && cargo test -p sonora -p sonora-aec3 --locked)`; `(cd vendor/sonora && cargo clippy -p sonora -p sonora-aec3 --all-targets --locked -- -D warnings)`; `(cd vendor/sonora && cargo fmt -p sonora-agc2 -p sonora-aec3 --check)`; `pnpm -C app prepare:tauri-assets --require-localvqe-assets`; `pnpm -C app tauri build --debug --ci --bundles app`; `pnpm -C app smoke:tauri-bundle`. `cargo audit` was not rerun locally because `cargo-audit` is not installed here; CI installs it before the audit gate.
- `cargo fmt --all --check` currently reaches `vendor/sonora`, which is documented as a read-only third-party fork and has pre-existing rustfmt drift. P0.0 used scoped formatting checks for first-party root packages and `app/src-tauri`.
- Tauri clippy/build still reports the known `block v0.1.6` future-incompatibility note; CODE_AUDIT tracks that under later dependency governance rather than P0.0.
- P0 was verified locally but the GitHub Actions matrix was not triggered; no push was performed.

### 2026-06-10 验收复核与跟进修复(见 `docs/audit/ACCEPTANCE_REVIEW.md`)

独立验收复核(三个 workspace 的 `clippy -D warnings` + `test` 全绿实测,非仅信本台账)后,对验收中发现的问题做了以下跟进修复:

- **ROB-4 残留**:`crates/echoless-cli/src/probe_delay.rs` 的 `spawn_probe_line_reader` 仍用 `lines().map_while(Result::ok)` 静默吞读取错误(此前 ROB-4 只修了 Tauri 侧,CLI 侧遗漏)。已改为显式 `match`,出错时打印 stderr 警告再停。
- **QUAL-1 延迟补偿**:节点边界 rubato `FftFixedIn` 的固有延迟此前未计入 `total_latency_ms()`(`chain.rs` 注释自承待补)。已加 `BoundaryAdapter::latency_ms()`(读 `output_delay()`),`total_latency_ms()` 累加主路径(near_in + near_out)的 SRC 延迟;realtime 启动前调 `chain.warm_up()` 预热,使该值在首帧前即准确。补了块边界连续性单测与 SRC 延迟计入单测。
- **PKG-1 退化包 fail-fast**:`app/scripts/prepare-tauri-assets.mjs` 此前缺 LocalVQE 资产时仅 warn,普通 `pnpm tauri build` 会静默产出退化包。已在非 dev 缺资产时打印醒目 banner(`--require-localvqe-assets` 仍作硬失败开关)。

验收备注(澄清,非代码改动):`library_candidates`(`crates/echoless-processors/src/localvqe.rs`)**未**拓宽至 `app_local_data`/打包资源目录;打包态由 Tauri 端注入 `ECHOLESS_LOCALVQE_LIBRARY` 绝对路径解决,backend 不自行搜 bundle。若未来需 backend 独立自救再拓宽。

仍待办(需 Windows 实机/CI,本地 macOS 无法闭环):Windows NSIS installer 安装后冒烟、Intel-mac sidecar 矩阵验证;`block v0.1.6` future-incompat 依赖治理(TEST-2)。
